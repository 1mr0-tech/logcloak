apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: logcloak-webhook
  labels:
    app.kubernetes.io/name: logcloak
    app.kubernetes.io/managed-by: {{ .Release.Service }}
webhooks:
- name: mutate.logcloak.io
  admissionReviewVersions: ["v1"]
  sideEffects: None
  failurePolicy: {{ .Values.webhook.failurePolicy }}
  timeoutSeconds: {{ .Values.webhook.timeoutSeconds }}
  clientConfig:
    service:
      name: logcloak-webhook
      namespace: {{ .Release.Namespace }}
      path: /mutate
    # caBundle is intentionally absent here — the webhook patches it at startup
    # via PatchWebhookCABundle. Keeping it out of the Helm template means Helm
    # never owns this field, so helm upgrade never conflicts with the webhook's patch.
  namespaceSelector:
    matchLabels:
      logcloak.io/inject: "true"
  rules:
  - apiGroups: [""]
    apiVersions: ["v1"]
    operations: ["CREATE"]
    resources: ["pods"]
